# Core
fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=21.2
celery>=5.4
redis>=5.0
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
httpx>=0.27
sqlalchemy>=2.0

# MCP / A2MCP (app/mcp/) - version not independently confirmed against
# PyPI at time of writing, same caveat as backend/mcp-gateway's @x402/*
# pins had; check before relying on it.
fastmcp>=2.0

# Geometry & Export
ezdxf>=1.3
shapely>=2.0
reportlab>=4.2
numpy>=1.26

# Storage & DB drivers
boto3>=1.34
psycopg2-binary>=2.9

# Dev / Test
pytest>=8.3
ruff>=0.6
black>=24.8
